- `relative_frequencies` are the marginal probabilities of corresponding binary
  variables
- `MAX_REJECTIONS` in `strategy_base` contains per-scenario maximum amount of
  rejections allowed; i.e. that's a hard constraint
